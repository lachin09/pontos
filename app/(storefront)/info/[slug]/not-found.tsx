import { NotFoundContent } from "@/components/layout/not-found-content";

/** Shown for info pages that do not exist or have no text yet. */
export default function InfoPageNotFound() {
  return <NotFoundContent />;
}
